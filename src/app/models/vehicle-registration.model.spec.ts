import { VehicleRegistration } from './vehicle-registration.model';

describe('VehicleRegistration', () => {
  it('should create an instance', () => {
    expect(new VehicleRegistration()).toBeTruthy();
  });
});
